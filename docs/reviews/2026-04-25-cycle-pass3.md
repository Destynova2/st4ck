# st4ck — cli-cycle pass 3

**Date:** 2026-04-25
**Audit:** `/cli-cycle` continuous improvement scan, full project
**Previous audits:**
- `2026-04-22-cycle-pass1.md` (Tier 3 only — 12 items)
- `2026-04-21-cycle-pass2.md` (full triage — 23 items)
**Output language:** English (detected from commits + README)

---

## Project Health

| Area                    | Score | Status                                                                   |
| ----------------------- | ----- | ------------------------------------------------------------------------ |
| Code Quality (HCL/TF)   | 8/10  | Hydra `random_id` resolved; `random_password` + `keepers` pattern landed |
| Shell Quality           | 8/10  | gen-configs.sh now exits with rc; cosign-sign.sh `set -eu` ok            |
| Doc-Code Sync           | 4/10  | Stack count, ADR count, version, KaaS coverage drift persists            |
| Documentation           | 7/10  | Diataxis intact, 26 ADRs, but architecture.md predates KaaS              |
| Test Quality            | 6/10  | `.tftest.hcl` now wired in CI (pass2 #10); still no integration runner   |
| Code Topology           | 8/10  | Stack-per-folder clean; 5 KaaS stacks scaffolded                         |
| README                  | 5/10  | Broken `docs/lld/` link unchanged + stale ADR count + Outscale phantom   |
| Project Structure       | 7/10  | `*.example` files at `stacks/` root; external-secrets has no `*.tf`     |
| Diagrams                | 7/10  | French text in english project (`docs/index.md`)                         |
| Pipeline                | 7/10  | Validate now covers all stacks; ext-secrets dir validates an empty TF    |
| Infrastructure          | 8/10  | KMS seal documented as accepted risk via ADR-026                         |
| Git Hygiene             | 4/10  | 30 modified/untracked files; 5 KaaS stacks never committed; 2 stashes    |
| **Overall**             | **6.6/10** |                                                                  |

---

## 🔴 Tier 3 — Critical (4 items)

| #   | Correction                                                                                                                                                                                                                                | Effort | Source         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------- |
| 1   | `stacks/external-secrets/` has **no `*.tf` files** (only `flux/` subdir) but is listed in `.woodpecker.yml:36` validate loop. `tofu validate` will fail with "No configuration files". Remove from loop OR add a guard `[ -f "$dir"/*.tf ]`. | Low    | pipeline+code  |
| 2   | 5 KaaS stacks (`stacks/{autoscaling,capi,gateway-api,kamaji,managed-cluster}`) and `contexts/dev-mgmt-fr-par.yaml` are **untracked in git** and listed in `.woodpecker.yml`. Next push to main will fail validate on missing dirs (or pass silently because of `[ -d ]` guard, hiding the gap). Commit them or remove from CI. | Medium | git+pipeline   |
| 3   | 18 modified files (backend.tf parameterization across all stacks) sitting unstaged. The new backend pattern requires `-backend-config` injection at init — if one engineer commits backend.tf without the matching Makefile change, every stack breaks at next `tofu init`. Stage and commit atomically.                | Low    | git            |
| 4   | 2 git stashes on `main` (`3afa3bc` "Phase A unstaged before brigade merge pull", `d1ed818` "untracked files on main"). Stashes on `main` are time bombs — drop or apply.                                                                  | Low    | git            |

## 🟡 Tier 2 — Major (10 items)

| #   | Correction                                                                                                                                                                                                                                                                                                       | Effort | Source     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------- |
| 5   | README.md:115 broken link `docs/lld/` — directory holds only `README.md`. Either populate it (LLDs for kamaji/capi/etc.) or change link to a concrete file. **Pass 1 #11 + Pass 2 #12 unresolved across 3 cycles.**                                                                                              | Low    | doc        |
| 6   | Stack count drift: README.md:12, README.md:25, AGENTS.md, CLAUDE.md "Stack Boundaries" table (only 8 listed), `docs/explanation/architecture.md:13`, `docs/explanation/bootstrap.md:174`, `docs/index.md:20`, `docs/techno.md:195`, `docs/roadmap.md:233`, `docs/tutorials/getting-started.md:93`, `docs/hld-talos-platform.md:178`, `docs/reference/commands.md:61` all say "7 stacks". Real on disk = 13 (8 tracked + 5 untracked). | Medium | sync       |
| 7   | "27 Helm charts" claim in README.md:12 + `docs/techno.md:195` is now ~40 (40 `helm_release` resources counted across stacks). Bump or remove the count.                                                                                                                                                          | Low    | sync       |
| 8   | CLAUDE.md "Stack Boundaries" table omits autoscaling, capi, kamaji, managed-cluster, gateway-api. **Pass 2 #13 unresolved.**                                                                                                                                                                                     | Medium | sync       |
| 9   | `docs/explanation/architecture.md` and `docs/hld-talos-platform.md` predate the KaaS pivot (no kamaji/capi/managed-cluster section). **Pass 2 #14 unresolved.**                                                                                                                                                  | High   | doc        |
| 10  | `docs/reference/commands.md` documents 69 `make` targets but the Makefile defines 123. Missing: `scaleway-bootstrap-vm`, `scaleway-fetch-creds`, `scaleway-tunnel-{start,stop}`, `scaleway-migrate-state-up`, `dr-backup{,-cnpg,-kms}`, `dr-verify-backup`, `brigade-tier3-{pass1,em-smoke}`, etc.                | Medium | sync       |
| 11  | README.md:117 "(22 ADRs)" — actual count is 26.                                                                                                                                                                                                                                                                  | Low    | sync       |
| 12  | README.md:125 "Provider-specific infra (Scaleway, local, **Outscale**, VMware)" — `envs/` contains `local`, `scaleway`, `vmware-airgap` only. No Outscale.                                                                                                                                                       | Low    | sync       |
| 13  | CONTRIBUTING.md:37 says "ADRs Written in French (project convention)". ADRs 023, 025, 026 are English. Either backfill 025/026 to French or change the convention. **Pass 2 #23 unresolved.**                                                                                                                    | Medium | sync       |
| 14  | CONTRIBUTING.md:45 references `make k8s-cni-test` which does not exist as a target. Phantom command in contributor onboarding.                                                                                                                                                                                   | Low    | sync       |

## 🟢 Tier 1 — Minor (8 items)

| #   | Correction                                                                                                                                                                                                                              | Effort | Source     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------- |
| 15  | `stacks/encryption-openbao.tf.example`, `stacks/encryption-pbkdf2.tf.example` at `stacks/` root — should live in a stack folder or `examples/`. **Pass 2 #19 unresolved.**                                                              | Low    | tree       |
| 16  | `docs/index.md:19-20` Mermaid still has French ("cluster 6 noeuds", "sequentiel ~15min") in an English-language project. **Pass 2 #17 unresolved.**                                                                                     | Low    | doc        |
| 17  | `tests/` directory only holds `velero/` chainsaw tests. No tofu test runner, no integration runner for the 13 stacks. Inverted pyramid. **Pass 2 #15 unresolved.**                                                                       | High   | test       |
| 18  | No `CHANGELOG.md` at repo root — 26 ADRs partly substitute. **Pass 2 #20 unresolved.**                                                                                                                                                  | Low    | doc        |
| 19  | `.pre-commit-config.yaml` is 103 bytes (gitleaks only) — consider adding `tflint`, `terraform fmt`, `shellcheck`, `markdownlint`. **Pass 2 #21 unresolved.**                                                                            | Low    | pipeline   |
| 20  | README Mermaid is a single 4-node flow; split into deploy + day-2 reconciliation diagrams. **Pass 2 #22 unresolved.**                                                                                                                   | Low    | schema     |
| 21  | New modules `modules/em-talos-bootstrap/` and `modules/em-talos-matchbox-bootstrap/` have **no entry** in CLAUDE.md / AGENTS.md / llms.txt / docs. Bare metal pivot is invisible to onboarding.                                          | Medium | doc        |
| 22  | `envs/scaleway/iam/terraform.tfstate{,.backup}` still on disk (gitignored, not committed). This stack uses local state instead of vault-backend. Migrate or document the deviation. **Pass 2 #16 unresolved.**                          | Low    | infra      |

---

## Strengths

- All container images SHA-pinned in `bootstrap/platform-pod.yaml` (matchbox sidecar pinned at v0.10.0).
- 26 numbered ADRs (4 added this cycle: 023 DR, 024 autoscaling, 025 kamaji, 026 static seal).
- Diataxis structure intact (tutorials/how-to/reference/explanation).
- Strict mode in 6/6 shell scripts (cosign uses POSIX `set -eu`).
- Provider-agnostic k8s stacks (`kubeconfig_path` only).
- Auto-generated secrets via `random_password` + `keepers` (Hydra fix landed).
- vault-backend → OpenBao KV v2 — backend.tf parameterized for multi-context (work in progress).
- `make scaleway-nuke` requires `CONFIRM=yes-destroy-everything` env var or interactive confirmation.
- `.woodpecker.yml` `start-builder` now has `path:` filter (no spurious billable VM spin-ups).
- 4 `.tftest.hcl` files now invoked in CI (`test-tftest` step).
- Conventional commit style enforced (last 20 commits: 100% compliant).
- `make state-snapshot` + DR runbook + 3-tier Garage backup chain (ADR-023).

---

## Trends

| Area                  | Pass 1 | Pass 2 | Pass 3 | Delta                    |
| --------------------- | ------ | ------ | ------ | ------------------------ |
| Code Quality          | —      | 7/10   | 8/10   | +1 (Hydra keepers)       |
| Shell Quality         | —      | 7/10   | 8/10   | +1 (gen-configs rc)      |
| Doc-Code Sync         | —      | 3/10   | 4/10   | +1 (CI loop fixed)       |
| Test Quality          | —      | 4/10   | 6/10   | +2 (tftest wired)        |
| Pipeline              | —      | 6/10   | 7/10   | +1 (path filter, tftest) |
| Infrastructure        | —      | 7/10   | 8/10   | +1 (ADR-026)             |
| README                | —      | 6/10   | 5/10   | -1 (new ADR count drift) |
| **Overall**           | —      | 6.4/10 | 6.6/10 | +0.2                     |

**Resolved across cycles:**
- Pass 1 #1 tfstate-in-git, #2 gen-configs FAIL, #4 OPENBAO_TOKEN root, #6 path filter, #7 tftest in CI, #12 Hydra keepers (false-positive: `random_password` not `random_id`).
- Pass 2 #1 gen-configs RC, #2 Hydra keepers, #3 OpenBao seal (ADR-026), #4 cosign-sign.sh (was already POSIX `set -eu`), #5 nuke confirmation, #9 validate covers all stacks, #10 tftest in CI, #11 path filter.

**Persistent across 2-3 cycles (red flags):**
- README `docs/lld/` broken link (Pass 1 #11, Pass 2 #12, Pass 3 #5).
- 7 stacks doc claim (Pass 2 #6, Pass 3 #6).
- French docs in English project (Pass 2 #17, Pass 3 #16).
- `.tftest.hcl` integration coverage (Pass 2 #15, Pass 3 #17).

---

## Phoenix status

The cycle is **NOT converged**: 4 critical (git hygiene) + 10 major (doc/code drift) items remain.

**Critical insight from this pass:** Git hygiene is the dominant risk vector. 30 uncommitted files including 5 entire stacks scaffolded but never landed mean the next contributor sees one repo, CI sees another, and `main` HEAD presents a third. The Phoenix burns drift; right now drift is being created faster than docs absorb it.

---

**Phoenix — Which tier should we tackle?**

| Choice | Action                                              | Items |
| ------ | --------------------------------------------------- | ----- |
| `1`    | Fix everything (autonomous convergence, unified plan) | 22    |
| `2`    | Fix 🔴 critical only                                | 4     |
| `3`    | Fix 🟡 major only                                   | 10    |
| `4`    | Fix 🟢 minor only                                   | 8     |
| `5`    | Not now                                             | —     |
