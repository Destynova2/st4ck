# st4ck — cli-cycle pass 2

**Date:** 2026-04-21
**Audit:** `/cli-cycle` continuous improvement scan, full project
**Previous audit:** `2026-04-22-cycle-pass1.md` (Tier 3 only — 12 items)
**Output language:** English (detected from commits + README)

---

## Project Health

| Area | Score | Status |
|------|-------|--------|
| Code Quality (HCL/TF) | 7/10 | Hydra `random_id` still missing `keepers` |
| Shell Quality | 7/10 | strict-mode set, but `gen-configs.sh` swallows FAIL |
| Doc-Code Sync | 3/10 | Massive drift: 7 vs 13 stacks, versions, phantom targets |
| Documentation | 7/10 | Good Diataxis, 25 ADRs, but stale stack list |
| Test Quality | 4/10 | `.tftest.hcl` exists but not wired in CI |
| Code Topology | 8/10 | Clean stack-per-folder, no god TF files |
| README | 6/10 | Broken `docs/lld/` link, stale badges |
| Project Structure | 8/10 | Clean, but `tests/` only holds Velero |
| Diagrams | 8/10 | Mermaid in README + 6 docs |
| Pipeline | 6/10 | Validate misses 6 new stacks; no test stage |
| Infrastructure | 7/10 | All images SHA-pinned; KMS seal still static |
| GitHub Repo | 6/10 | No GH Actions (Woodpecker self-hosted) |
| **Overall** | **6.4/10** | |

---

## 🔴 Tier 3 — Critical (5 items)

| # | Correction | Effort | Source |
|---|-----------|--------|--------|
| 1 | `envs/vmware-airgap/scripts/gen-configs.sh:182-188` — `talosctl validate` failure prints "FAIL" but loop continues; script exits 0 even when configs are invalid. Fix: track `RC=1`, `exit "$RC"` at end. | Low | shell+code |
| 2 | Hydra `random_id` (sourced from `stacks/pki/` per `stacks/identity/main.tf:31`) likely still missing `keepers`. Without keepers, any plan-changing edit regenerates the secret and breaks running Hydra sessions. | Low | code |
| 3 | OpenBao seal key plaintext — `seal "static"` reads `file:///bao/seal/unseal.key`. No KMS wrap. No ADR documents the gap explicitly. Either KMS-wrap (Scaleway KMS) or file an ADR-026 "static seal accepted risk". | Medium | infra |
| 4 | `scripts/cosign-sign.sh` uses `#!/bin/sh` with no `set -e` — fails silently on signing errors in a security pipeline. | Low | shell |
| 5 | Verify `make scaleway-nuke` requires `CONFIRM=YES` or interactive prompt before running destructive teardown. | Low | shell |

## 🟡 Tier 2 — Major (10 items)

| # | Correction | Effort | Source |
|---|-----------|--------|--------|
| 6 | Doc-code drift on stack count: README/CLAUDE.md/AGENTS.md/llms.txt/docs/index.md say "7 stacks". Reality: 13 (autoscaling, capi, cni, external-secrets, flux-bootstrap, gateway-api, identity, kamaji, managed-cluster, monitoring, pki, security, storage). | Medium | sync+doc |
| 7 | Version drift in 12+ docs. `vars.mk` = Talos `v1.12.6` + K8s `1.35.4`. README badges, AGENTS.md, techno.md, hld-talos-platform.md, MULTI-ENV.md, NAMING.md, roadmap.md, design/001-hld-lld.md, reference/commands.md, reference/config.md all still `v1.12.4` + `1.35.0`. | Low | sync |
| 8 | 16 phantom Makefile targets in `docs/reference/commands.md`: `scaleway-bootstrap`, `scaleway-demo`, `scaleway-oidc`, `capi-{init,create-cpu,create-gpu,status,kubeconfig,delete,destroy}`, `scaleway-{test,iam-test,image-test,cluster-test}`, `k8s-cni-test`, `cilium-manifests`. None exist. | Medium | sync |
| 9 | `.woodpecker.yml` `validate` step iterates only 9 dirs. Missing 6 new stacks: autoscaling, capi, kamaji, managed-cluster, gateway-api, external-secrets. New code lands without `tofu validate`. | Low | pipeline |
| 10 | 4 `.tftest.hcl` files (iam, image, ci, cluster) still not invoked in CI. Pass 1 #7 unresolved. | Low | pipeline+test |
| 11 | `.woodpecker.yml` `start-builder` still has no `path:` filter — billable Scaleway VM spins up on every push, including doc-only commits. Pass 1 #6 unresolved. | Low | pipeline |
| 12 | Broken link in README.md:115 → `docs/lld/` directory does not exist. Pass 1 #11 unresolved. | Low | doc |
| 13 | `CLAUDE.md` "Stack Boundaries" table omits capi, kamaji, managed-cluster, autoscaling, gateway-api. | Medium | sync |
| 14 | `docs/explanation/architecture.md` and `docs/hld-talos-platform.md` predate the KaaS pivot (kamaji, capi, ADR-024). Cross-check + rewrite needed. | High | doc |
| 15 | `tests/` only holds Velero. No tofu test runner, no integration runner for the 13 stacks. Inverted pyramid. | High | test |

## 🟢 Tier 1 — Minor (8 items)

| # | Correction | Effort | Source |
|---|-----------|--------|--------|
| 16 | `envs/scaleway/iam/terraform.tfstate{,.backup}` exist on disk (gitignored, NOT committed — Pass 1 #1 was a false positive). This stack uses local state instead of vault-backend. Migrate or document. | Low | infra |
| 17 | `docs/index.md` Mermaid has French text ("cluster 6 noeuds", "sequentiel") in an English-language project. | Low | doc |
| 18 | `.gitleaks.toml` allowlist could expand to known false-positive paths (chart fixtures, test data). | Low | shell |
| 19 | `stacks/encryption-openbao.tf.example`, `stacks/encryption-pbkdf2.tf.example` at `stacks/` root — should live in a stack folder or `examples/`. | Low | tree |
| 20 | No `CHANGELOG.md` at repo root — 25 ADRs partly substitute. | Low | doc |
| 21 | `.pre-commit-config.yaml` is 103 bytes — consider adding `tflint`, `terraform fmt`, `shellcheck`, `markdownlint`. | Low | pipeline |
| 22 | README Mermaid is a 4-node flow; split into deploy + day-2 reconciliation diagrams. | Low | schema |
| 23 | `CONTRIBUTING.md` says ADRs are French; recent ADRs (023, 024, 025) are English — update convention. | Low | sync |

---

## Strengths

- All container images SHA-pinned in `bootstrap/platform-pod.yaml`.
- 25 numbered ADRs.
- Diataxis structure applied.
- Strict mode in 5/6 shell scripts.
- Provider-agnostic k8s stacks (`kubeconfig_path` only).
- Auto-generated secrets via `random_id` — zero secrets in git.
- vault-backend → OpenBao KV v2 across all 13 stacks.
- Mermaid diagrams in 9 doc files + README.
- Conventional commit style enforced in recent history.

---

## Trends vs Pass 1

| Pass 1 finding | Status |
|---|---|
| #1 tfstate committed | FALSE POSITIVE — gitignored, not in git |
| #2 gen-configs.sh exit-0 | OPEN |
| #3 k8s-down confirmation | MOVED to `scaleway-nuke` (verify) |
| #4 OPENBAO_TOKEN=root | CLOSED — no `"root"` default found |
| #5 OpenBao seal KMS-wrap | OPEN |
| #6 .woodpecker path filter | OPEN |
| #7 .tftest.hcl in CI | OPEN |
| #8 standalone OpenBao claim | CLOSED |
| #9 doc version bump | OPEN |
| #10 phantom make targets | OPEN (16 confirmed) |
| #11 broken `docs/lld/` link | OPEN |
| #12 Hydra random_id keepers | OPEN |

**Resolved:** 3/12 (25%). **Open:** 9/12. **New this pass:** 11 (mostly KaaS doc drift).

---

## Phoenix Status

NOT CONVERGED. 5 critical + 10 major remain. Recent KaaS work (kamaji, capi, managed-cluster, autoscaling, gateway-api, ADR-023/024) is excellent engineering but outpaced documentation. Next pass should focus on doc re-sync.
