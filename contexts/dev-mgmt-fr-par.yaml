# Management cluster — first real Talos cluster on Scaleway fr-par.
# Hosts the Kamaji KaaS control plane (capi + kamaji + autoscaling + gateway-api).
# Tenant clusters use contexts/tenant-*-fr-par.yaml.

env: dev
instance: mgmt
region: fr-par

owner: ludwig

management_cidrs:
  - "91.161.223.144/32"
