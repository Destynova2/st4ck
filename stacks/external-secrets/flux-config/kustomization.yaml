apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ESO post-install config — depends on the CRDs created by
# ../flux/helmrelease.yaml. Applied by the `management` Flux
# Kustomization (which dependsOn: management-eso).
resources:
  - cluster-secret-store.yaml
