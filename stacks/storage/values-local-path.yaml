# local-path-provisioner — default StorageClass
# Upstream Rancher chart (deploy/chart/local-path-provisioner)

storageClass:
  name: local-path
  defaultClass: true

nodePathMap:
  - node: DEFAULT_PATH_FOR_NON_LISTED_NODES
    paths:
      - /var/local-path-provisioner
