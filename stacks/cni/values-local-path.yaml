# local-path-provisioner — default StorageClass
# containeroo Helm chart wrapping Rancher's local-path-provisioner

storageClass:
  name: local-path
  defaultClass: true

nodePathMap:
  - node: DEFAULT_PATH_FOR_NON_LISTED_NODES
    paths:
      - /var/local-path-provisioner
